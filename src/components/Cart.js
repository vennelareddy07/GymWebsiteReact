let Cart=(props)=>{
      let menCart=props.menCart
      console.log(menCart)
    return(
        <div>
            {/* {
                menCart.map((item)=>{
                    return(
                        <div>{item.Title}</div>
                    )
                })
            } */}
        </div>
    )
}
export default Cart